require("notify").setup ({
    background_colour = "#000000",
    fps = 60,
    level = 2,
    minimum_width = 50,
    render = "minimal", --minimal or default
    stages = "slide",
    timeout = 4000
})
